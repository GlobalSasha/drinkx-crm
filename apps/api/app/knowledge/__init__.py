"""Knowledge Base CRUD domain placeholder.

Static markdown KB lives in `apps/api/knowledge/` (loaded by
`app/enrichment/profile.py` + `app/enrichment/sources/`). This package
slot is reserved for the upcoming Knowledge Base admin UI
(Sprint 2.8+ long-tail) — editing playbooks / ICP / competitor
notes from inside the CRM rather than via git PRs.
"""
