"""Message Templates domain — Sprint 2.4 G4.

Reusable message bodies (email / tg / sms) for the upcoming Automation
Builder (Sprint 2.5). G4 ships data model + admin CRUD only — no
rendering, no outbound dispatch.

Package name is `template` (singular) per CLAUDE.md domain registry;
the REST surface mounts at `/api/templates` (plural) — that's a
routing convention, not a package-naming one.
"""
