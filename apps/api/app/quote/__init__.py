"""Quote / КП-builder domain placeholder.

Listed in PRD-v2.0 + CLAUDE.md domain registry but not yet implemented
(Sprint 2.8+ long-tail). Live КП templates currently flow through
`app/template/` + `app/automation_builder/` send-template actions.
Promote to a real package (models/schemas/repositories/services/routers)
when the Quote-builder UI lands; until then leave empty so the domain
slot stays reserved.
"""
