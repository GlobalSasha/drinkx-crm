"""Lead assignment domain placeholder.

Assignment logic currently lives inline in `app/leads/services.py`
(claim / transfer / pool ↔ assigned transitions). This slot is reserved
for the dedicated assignment package — round-robin / territory rules /
workload-aware routing — when those land (Phase 3).
"""
