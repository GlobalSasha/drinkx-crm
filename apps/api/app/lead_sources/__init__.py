"""Lead-source dictionary domain — workspace-configurable list of «откуда появился лид».

Mirrors the Pipeline/Stage configurable-list pattern (admin-CRUD, seed defaults)
rather than the auto-create UTM dictionary: sources here are curated by the head
and picked by managers in the lead-create form, not derived from web params.
"""
